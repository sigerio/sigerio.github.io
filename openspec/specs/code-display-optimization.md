# 代码段/命令行段/公式段显示优化方案

## 状态
- 类型：implemented
- 日期：2024
- 方案：轻量优化（不引入新依赖）

## 目标
保持现有页面格式不变，优化代码段、命令行段和公式段的显示规则。

## 约束
- ❌ 禁止改动 `_posts` 文件夹内的内容（除非用户主动提出）
- ❌ 不引入新的第三方依赖

## 优化内容

### 1. 代码块优化
- 添加复制按钮（纯JS实现）
- 复制成功后显示提示

### 2. 命令行样式区分
- 识别 `language-bash`、`language-shell`、`language-terminal` 类名
- 添加命令提示符样式（$）
- 区分命令和输出

### 3. 公式块优化
- 块级公式居中显示
- 优化公式上下间距
- 公式溢出时支持横向滚动

## 涉及文件
- `assets/css/main.css` - 样式修改
- `assets/js/main.js` - JS功能添加

## 实现细节

### CSS 新增样式
```css
/* 代码块复制按钮 */
/* 命令行特殊样式 */
/* 公式块样式 */
```

### JS 新增功能
```javascript
/* 代码块复制功能 */
```
