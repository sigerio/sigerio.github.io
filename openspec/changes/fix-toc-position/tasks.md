# 任务清单：固定文章目录位置

## 变更标识
- **change-id**: `fix-toc-position`

## 任务列表

### 阶段1：修改定位方式 [高优先级]

- [ ] **T1.1** 将 `.post-toc` 从 `sticky` 改为 `fixed` 定位
- [ ] **T1.2** 计算并设置正确的 `right` 位置值
- [ ] **T1.3** 确保 `max-height` 和 `overflow-y` 正确设置

### 阶段2：优化滚动条样式 [中优先级]

- [ ] **T2.1** 添加 webkit 滚动条自定义样式
- [ ] **T2.2** 添加 Firefox 滚动条样式（scrollbar-width, scrollbar-color）

### 阶段3：提交变更 [必要]

- [ ] **T3.1** 提交变更并推送

## 预估工作量

| 阶段 | 预估时间 |
|------|----------|
| 阶段1 | 10 分钟 |
| 阶段2 | 5 分钟 |
| 阶段3 | 5 分钟 |
| **总计** | **20 分钟** |
